# Load topology on boot
load_definitions = /etc/rabbitmq/definitions.json

# Management plugin listener (bound to localhost via compose port mapping)
management.tcp.port = 15672

# Keep logs readable
log.console = true
log.console.level = info
